# Weather Channel Interview Exercise

## Instructions

To run, type: `npm install && bower install && npm start`

To build the "source", you must have gulp installed.

Use "gulp watch" to build app and watch html, scss, and src directories.